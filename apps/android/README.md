# Sukavina Android

Ứng dụng native viết hoàn toàn bằng Kotlin và Jetpack Compose, không sử dụng WebView.

Mở thư mục này trong Android Studio. Preview nằm cuối `SukavinaApp.kt`. Chạy `gradle lintDebug assembleDebug` để kiểm tra và tạo APK debug.
