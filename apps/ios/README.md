# Sukavina iOS

Ứng dụng native viết bằng Swift và SwiftUI.

Mở `App/App.xcodeproj`, chọn `AppDelegate.swift`, sau đó bật Xcode Canvas để xem preview không cần API. Build thiết bị thật vẫn cần cấu hình ký ứng dụng Apple phù hợp; workflow CI chỉ tạo IPA unsigned.
